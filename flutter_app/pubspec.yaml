name: tamtom
description: تطبيق طمطوم الأصلي - متجر الخضار والفواكه
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  webview_flutter: ^4.8.0
  webview_flutter_android: ^4.0.0
  webview_flutter_wkwebview: ^3.13.0

  connectivity_plus: ^6.0.0
  url_launcher: ^6.3.0
  http: ^1.2.0
  shared_preferences: ^2.2.2

  provider: ^6.1.2
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
